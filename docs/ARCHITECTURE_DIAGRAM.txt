╔═══════════════════════════════════════════════════════════════════════════════════════════════╗
║                    AWS GRAVITON VALIDATOR - PRODUCTION ARCHITECTURE                           ║
║                    Account: prushok+graviton-Admin | Region: us-east-1                        ║
║                    Deployment: graviton-validator-vjqx4x                                      ║
╚═══════════════════════════════════════════════════════════════════════════════════════════════╝

┌─────────────┐
│  Developer  │
│   (User)    │
└──────┬──────┘
       │ 1. Upload SBOM
       │ (aws s3 cp)
       ▼
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│                         S3 Bucket: graviton-validator-vjqx4x                                 │
│                         🔒 KMS Encrypted | 📦 Versioned | ⏰ 90-day lifecycle                │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  📁 input/individual/  ← Single SBOM files (.json)                                           │
│  📁 input/batch/       ← Batch manifest + multiple SBOMs                                     │
│  📁 output/            ← Analysis results (Excel, JSON, Markdown)                            │
│  📁 code/              ← Validator tool package (migration-accelerator-graviton.zip)         │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
       │ 2. S3 Event Notification
       │ (EventBridge enabled)
       ▼
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│                              Amazon EventBridge Rules                                         │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  📋 graviton-validator-individual-vjqx4x                                                     │
│     Pattern: input/individual/*.json → Triggers on single SBOM upload                        │
│                                                                                              │
│  📋 graviton-validator-batch-vjqx4x                                                          │
│     Pattern: **/batch-manifest.txt → Triggers on batch manifest upload                       │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
       │ 3. Invoke Lambda
       ▼
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│              Lambda Function: graviton-validator-batch-trigger-vjqx4x                        │
│              Runtime: Python 3.11 | Timeout: 60s | Memory: 128MB                             │
│              VPC: vpc-032193697daff2f33 (Private Subnets)                                    │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  Features:                                                                                   │
│  ✓ X-Ray Tracing: Active                    ✓ Code Signing: Enabled                         │
│  ✓ Dead Letter Queue: SQS                   ✓ Reserved Concurrency: 50                      │
│  ✓ KMS Encryption: Environment variables    ✓ VPC-attached (private subnets)                │
│                                                                                              │
│  Logic:                                                                                      │
│  1. Parse S3 event → Determine mode (individual/batch)                                      │
│  2. Extract SBOM metadata and project info                                                  │
│  3. Check for duplicate running jobs (idempotent)                                           │
│  4. Submit AWS Batch job with appropriate parameters                                        │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
       │ 4. Submit Job
       ▼
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│                   AWS Batch Job Queue: graviton-validator-queue-vjqx4x                       │
│                   Priority: 1 | State: ENABLED                                               │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
       │ 5. Schedule Job
       ▼
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│              Compute Environment: graviton-validator-vjqx4x                                  │
│              Type: MANAGED | State: ENABLED                                                  │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│  Instance: m7g.xlarge (Graviton3 - ARM64)                                                    │
│  • 4 vCPUs, 16GB RAM per instance                                                            │
│  • Min vCPUs: 0 (scales to zero when idle) 💰                                               │
│  • Max vCPUs: 20 (max 5 concurrent instances)                                               │
│  • Auto-scaling: Dynamic based on queue depth                                               │
│                                                                                              │
│  Network:                                                                                    │
│  • VPC: vpc-032193697daff2f33 (10.0.0.0/16)                                                 │
│  • Subnets: Private subnets in us-east-1a, us-east-1b (Multi-AZ)                           │
│  • NAT Gateway: Single NAT for internet access                                              │
│  • Security Group: Egress-only (HTTPS, HTTP, Ephemeral ports)                              │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
       │ 6. Launch Container
       ▼
╔══════════════════════════════════════════════════════════════════════════════════════════════╗
║                    Batch Job Container (Amazon Linux 2023)                                   ║
║                    Job Definition: graviton-validator-job-vjqx4x                             ║
╚══════════════════════════════════════════════════════════════════════════════════════════════╝

   ┌────────────────────────────────────────────────────────────────────────────────────────┐
   │ STEP 1: Environment Setup                                                              │
   ├────────────────────────────────────────────────────────────────────────────────────────┤
   │ • Install: Python 3.11, Docker, AWS CLI, jq, git                                       │
   │ • Start Docker daemon (privileged mode for Docker-in-Docker)                           │
   │ • Retrieve IAM credentials from EC2 instance metadata (IMDSv2)                         │
   └────────────────────────────────────────────────────────────────────────────────────────┘
                                          ↓
   ┌────────────────────────────────────────────────────────────────────────────────────────┐
   │ STEP 2: Download Assets from S3                                                        │
   ├────────────────────────────────────────────────────────────────────────────────────────┤
   │ • Download: s3://graviton-validator-vjqx4x/code/migration-accelerator-graviton.zip    │
   │ • Extract validator tool                                                               │
   │ • Install Python dependencies (pip install -r requirements.txt)                        │
   │ • Download SBOM file(s) from input/ folder                                             │
   └────────────────────────────────────────────────────────────────────────────────────────┘
                                          ↓
   ┌────────────────────────────────────────────────────────────────────────────────────────┐
   │ STEP 3: Run Graviton Compatibility Analysis                                            │
   ├────────────────────────────────────────────────────────────────────────────────────────┤
   │ Command: python3.11 graviton_validator.py [SBOM] --runtime --test --containers        │
   │                                                                                         │
   │ Analysis Process:                                                                       │
   │ 1. Parse SBOM (CycloneDX/SPDX format)                                                  │
   │ 2. Detect package managers (Maven, npm, pip, NuGet, Gem)                               │
   │ 3. Build Docker containers for each runtime (Java, Node.js, Python, .NET, Ruby)        │
   │ 4. Install packages on ARM64 architecture                                              │
   │ 5. Run compatibility tests                                                             │
   │ 6. Detect native modules (.so, .node, .dll files)                                      │
   │ 7. Generate compatibility report with recommendations                                  │
   │                                                                                         │
   │ Output Files:                                                                           │
   │ • Excel report (.xlsx) - Detailed compatibility matrix                                 │
   │ • JSON analysis - Machine-readable results                                             │
   │ • Markdown report - Human-readable summary                                             │
   │ • Runtime logs - Per-language analysis details                                         │
   └────────────────────────────────────────────────────────────────────────────────────────┘
                                          ↓
   ┌────────────────────────────────────────────────────────────────────────────────────────┐
   │ STEP 4: Upload Results to S3                                                           │
   ├────────────────────────────────────────────────────────────────────────────────────────┤
   │ • Individual mode: s3://graviton-validator-vjqx4x/output/individual/<sbom-name>/       │
   │ • Batch mode: s3://graviton-validator-vjqx4x/output/batch/<project-name>/              │
   │                                                                                         │
   │ Files uploaded:                                                                         │
   │ • <name>-report.xlsx                                                                    │
   │ • <name>_merged_analysis.json                                                           │
   │ • <runtime>/<name>_<runtime>_analysis.json (per language)                              │
   │ • <runtime>/<name>_<manifest> (package manifests)                                       │
   └────────────────────────────────────────────────────────────────────────────────────────┘
                                          ↓
   ┌────────────────────────────────────────────────────────────────────────────────────────┐
   │ STEP 5: Job Completion & Cleanup                                                       │
   ├────────────────────────────────────────────────────────────────────────────────────────┤
   │ • Write logs to CloudWatch: /aws/batch/graviton-validator/job/default/<job-id>         │
   │ • Job status: SUCCEEDED or FAILED                                                      │
   │ • EC2 instance terminates (auto-scaling to zero)                                       │
   │ • No cost when idle 💰                                                                  │
   └────────────────────────────────────────────────────────────────────────────────────────┘

       │ 7. Download Results
       ▼
┌─────────────┐
│  Developer  │
│   (User)    │
└─────────────┘

╔═══════════════════════════════════════════════════════════════════════════════════════════════╗
║                                  MONITORING & SECURITY                                        ║
╚═══════════════════════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────┐  ┌──────────────────────────────────┐  ┌─────────────────┐
│      CloudWatch Monitoring       │  │         KMS Encryption           │  │   IAM Roles     │
├──────────────────────────────────┤  ├──────────────────────────────────┤  ├─────────────────┤
│ • Dashboard: Real-time metrics   │  │ • S3 Key: 7f2aaf6f-e885...       │  │ • Lambda Role   │
│ • Logs: 365-day retention        │  │ • EBS Key: 9168639c-42a0...      │  │ • Batch Service │
│ • Alarms: Job failure detection  │  │ • Auto-rotation: Enabled         │  │ • Batch Job     │
│ • X-Ray: Distributed tracing     │  │ • Usage: S3, Logs, Lambda, SQS   │  │ • Batch EC2     │
└──────────────────────────────────┘  └──────────────────────────────────┘  └─────────────────┘

╔═══════════════════════════════════════════════════════════════════════════════════════════════╗
║                                    KEY FEATURES                                               ║
╠═══════════════════════════════════════════════════════════════════════════════════════════════╣
║ ✓ Multi-AZ Deployment (us-east-1a, us-east-1b)    ✓ Auto-scaling to zero (no idle cost)     ║
║ ✓ KMS Encryption (at rest + in transit)           ✓ VPC Isolation (private subnets)          ║
║ ✓ X-Ray Tracing enabled                           ✓ CloudWatch monitoring + alarms           ║
║ ✓ Code Signing (Lambda)                           ✓ Spot instance support (90% savings)      ║
║ ✓ S3 Versioning + Lifecycle policies              ✓ IMDSv2 for EC2 metadata                  ║
║ ✓ Dead Letter Queue (SQS)                         ✓ Least-privilege IAM roles                ║
╚═══════════════════════════════════════════════════════════════════════════════════════════════╝
